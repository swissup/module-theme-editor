<?php
    $columnsWidth = $block->getContentWidth() / 1920 * 100;
    $headerWidth = $block->getHeaderWidth() / 1920 * 100;
    $footerWidth = $block->getFooterWidth() / 1920 * 100;
?>
<style>
    ._header {
        max-width: <?= /* @noEscape */ $headerWidth ? ($headerWidth . '%') : 'none' ?>;
    }
    ._columns {
        max-width: <?= /* @noEscape */ $columnsWidth ?>%;
    }

    ._footer {
        max-width: <?= /* @noEscape */ $footerWidth ? ($footerWidth . '%') : 'none' ?>;
    }
</style>
